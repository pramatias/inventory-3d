[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "inventory-3d-geometry"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
strip = true
module-name = "_geometry"
